export default class CelebrityDisplay {
  constructor (id, name, description, poster, birth, death, professions, knownFor) {
    this.id = id
    this.name = name
    this.description = description
    this.poster = poster
    this.birth = birth
    this.death = death
    this.professions = professions
    this.knownFor = knownFor
  }
}
