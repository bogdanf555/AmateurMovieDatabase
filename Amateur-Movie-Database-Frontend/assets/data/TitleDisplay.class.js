export default class TitleDislay {
  constructor (id, name, poster, plot, year, votes, duration, rating, genres, directors, writers, stars) {
    this.id = id
    this.name = name
    this.plot = plot
    this.year = year
    this.poster = poster
    this.votes = votes
    this.duration = duration
    this.genres = genres
    this.directors = directors
    this.writers = writers
    this.stars = stars
    this.rating = rating
  }
}
